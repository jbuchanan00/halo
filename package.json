{
  "name": "halo",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:up": "docker compose -f server/docker-compose.dev.yml down --remove-orphans && docker compose -f server/docker-compose.dev.yml up -d --build && docker compose -f halo-client/docker-compose.dev.yml down --remove-orphans && docker compose -f halo-client/docker-compose.dev.yml up -d --build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "description": ""
}
